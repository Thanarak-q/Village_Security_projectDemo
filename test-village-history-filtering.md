# 🧪 การทดสอบการกรองประวัติผู้เยี่ยมตามหมู่บ้าน

## 📋 สรุปการเปลี่ยนแปลง

### ✅ Backend (พร้อมใช้งานแล้ว)
- **API Endpoint**: `/api/visitor-records/village/:village_key`
- **Function**: `getVisitorRecordsByVillage(villageKey)` ใน `visitorRecordUtils.ts`
- **การทำงาน**: กรองข้อมูลจากตาราง `visitor_records` ผ่าน JOIN กับ `houses` table ตาม `village_key`

### ✅ Frontend (อัปเดตแล้ว)
- **การดึงข้อมูล**: ใช้ `sessionStorage.getItem('selectedVillage')` เพื่อดึง village_key
- **API Call**: เรียก `/api/visitor-records/village/${villageKey}` แทน `/api/visitor-records`
- **UI Enhancement**: 
  - แสดงชื่อหมู่บ้านที่เลือกใน Visitor History tab
  - เพิ่มปุ่มรีเฟรช
  - Auto-refresh เมื่อเปลี่ยนหมู่บ้าน

---

## 🔧 วิธีการทดสอบ

### 1. **ทดสอบการกรองข้อมูล**

#### วิธีที่ 1: ผ่าน UI
1. เข้าสู่ระบบ Admin Dashboard
2. เลือกหมู่บ้านจากเมนู "เปลี่ยนหมู่บ้าน"
3. ไปที่หน้า "ประวัติ" 
4. คลิกที่ tab "ประวัติผู้เยี่ยม"
5. ตรวจสอบว่าแสดงข้อมูลเฉพาะหมู่บ้านที่เลือก

#### วิธีที่ 2: ผ่าน API โดยตรง
```bash
# ทดสอบดึงข้อมูลทั้งหมด
curl -X GET "http://localhost:3001/api/visitor-records" \
  -H "Content-Type: application/json"

# ทดสอบดึงข้อมูลตามหมู่บ้าน
curl -X GET "http://localhost:3001/api/visitor-records/village/VILLAGE_KEY_HERE" \
  -H "Content-Type: application/json"
```

### 2. **ทดสอบการ Auto-refresh**

1. เปิดหน้า History ใน browser
2. เปลี่ยนหมู่บ้านใน tab อื่น
3. กลับมาที่หน้า History
4. ตรวจสอบว่าข้อมูลอัปเดตอัตโนมัติ

### 3. **ทดสอบปุ่มรีเฟรช**

1. คลิกปุ่ม "รีเฟรช" ในหน้า History
2. ตรวจสอบว่าแสดง loading indicator
3. ตรวจสอบว่าข้อมูลอัปเดต

---

## 🎯 ผลลัพธ์ที่คาดหวัง

### ✅ เมื่อเลือกหมู่บ้าน A
- แสดงเฉพาะประวัติผู้เยี่ยมของหมู่บ้าน A
- แสดงชื่อหมู่บ้าน A ใน Visitor History tab
- จำนวนรายการตรงกับข้อมูลในหมู่บ้าน A

### ✅ เมื่อเลือกหมู่บ้าน B  
- แสดงเฉพาะประวัติผู้เยี่ยมของหมู่บ้าน B
- แสดงชื่อหมู่บ้าน B ใน Visitor History tab
- จำนวนรายการตรงกับข้อมูลในหมู่บ้าน B

### ✅ เมื่อไม่เลือกหมู่บ้าน (Superadmin)
- แสดงข้อมูลทั้งหมด (fallback ไปที่ `/api/visitor-records`)
- ไม่แสดงชื่อหมู่บ้านใน tab

---

## 🐛 การแก้ไขปัญหาที่อาจเกิดขึ้น

### ❌ ไม่แสดงข้อมูล
1. ตรวจสอบ `sessionStorage.getItem('selectedVillage')`
2. ตรวจสอบ API endpoint ทำงานถูกต้อง
3. ตรวจสอบ Console logs สำหรับ error messages

### ❌ แสดงข้อมูลผิดหมู่บ้าน
1. ตรวจสอบ `village_key` ใน database
2. ตรวจสอบ JOIN condition ใน SQL query
3. ตรวจสอบ sessionStorage value

### ❌ Auto-refresh ไม่ทำงาน
1. ตรวจสอบ `villageChanged` event listener
2. ตรวจสอบ `fetchHistory(true)` function call
3. ตรวจสอบ Console logs

---

## 📊 ข้อมูลสำหรับการทดสอบ

### ตัวอย่าง Village Keys
```sql
-- ตรวจสอบ village keys ที่มีในระบบ
SELECT village_key, village_name FROM villages;
```

### ตัวอย่าง Visitor Records
```sql
-- ตรวจสอบ visitor records แยกตามหมู่บ้าน
SELECT 
  vr.visitor_record_id,
  vr.license_plate,
  h.village_key,
  v.village_name
FROM visitor_records vr
JOIN houses h ON vr.house_id = h.house_id
JOIN villages v ON h.village_key = v.village_key
ORDER BY h.village_key, vr.createdAt DESC;
```

---

## 🚀 การใช้งานจริง

### สำหรับ Admin/Staff
- ระบบจะกรองข้อมูลตามหมู่บ้านที่เลือกอัตโนมัติ
- สามารถเปลี่ยนหมู่บ้านได้จากเมนู "เปลี่ยนหมู่บ้าน"
- ข้อมูลจะอัปเดตทันทีเมื่อเปลี่ยนหมู่บ้าน

### สำหรับ Superadmin
- สามารถดูข้อมูลทั้งหมดได้ (ไม่กรองตามหมู่บ้าน)
- หรือเลือกหมู่บ้านเฉพาะเพื่อดูข้อมูลแยกตามหมู่บ้าน

---

## ✅ Checklist การทดสอบ

- [ ] API endpoint `/api/visitor-records/village/:village_key` ทำงานถูกต้อง
- [ ] Frontend ดึงข้อมูลตาม village_key ที่เลือก
- [ ] แสดงชื่อหมู่บ้านใน Visitor History tab
- [ ] Auto-refresh เมื่อเปลี่ยนหมู่บ้าน
- [ ] ปุ่มรีเฟรชทำงานถูกต้อง
- [ ] กรองข้อมูลตามสถานะ (pending, approved, rejected)
- [ ] ค้นหาตามทะเบียนรถ
- [ ] Pagination ทำงานถูกต้อง
- [ ] Responsive design บน mobile

---

## 📝 หมายเหตุ

- ข้อมูล Admin History ยังเป็น dummy data (ยังไม่ได้เชื่อมต่อ API)
- ระบบรองรับการทำงานแบบ offline (fallback)
- มี error handling สำหรับกรณี API ไม่ตอบสนอง
- Console logs ช่วยในการ debug และ monitoring
