# Visitor Notification System

ระบบแจ้งเตือนผู้เยี่ยมผ่าน LINE Flex Message สำหรับผู้อยู่อาศัยในหมู่บ้าน

## ภาพรวมระบบ

ระบบนี้ช่วยให้รปภ.สามารถส่งแจ้งเตือนผู้เยี่ยมไปยังผู้อยู่อาศัยในบ้านเลขที่เดียวกันผ่าน LINE Flex Message โดยผู้อยู่อาศัยสามารถ:

- ✅ **ยืนยันการเข้า** - อนุญาตให้ผู้เยี่ยมเข้าบ้านได้
- ❌ **ปฏิเสธ** - ไม่อนุญาตให้ผู้เยี่ยมเข้าบ้าน พร้อมระบุเหตุผล
- 📋 **ดูรายละเอียด** - ดูข้อมูลผู้เยี่ยมแบบละเอียดก่อนตัดสินใจ

## ฟีเจอร์หลัก

### 1. การแจ้งเตือนผู้เยี่ยม
- ส่งแจ้งเตือนไปยังผู้อยู่อาศัยทุกคนในบ้านเลขที่เดียวกัน
- แสดงข้อมูลผู้เยี่ยมเบื้องต้น
- ปุ่มยืนยันการเข้าและดูรายละเอียด

### 2. การดูรายละเอียด
- ข้อมูลผู้เยี่ยมแบบละเอียด
- รูปภาพผู้เยี่ยม (ถ้ามี)
- ปุ่มยืนยันและปฏิเสธ

### 3. การตอบกลับ
- ยืนยันการเข้า
- ปฏิเสธพร้อมเหตุผล
- แจ้งเตือนยืนยันกลับไปยังผู้อยู่อาศัย

## API Endpoints

### 1. ส่งแจ้งเตือนไปยังผู้อยู่อาศัย
```http
POST /api/visitor-notification/send-to-residents
Authorization: Bearer <JWT_TOKEN>
Content-Type: application/json

{
  "visitorName": "สมชาย ใจดี",
  "visitorPhone": "081-234-5678",
  "houseNumber": "123/45",
  "residentName": "สมหญิง รักบ้าน",
  "purpose": "เยี่ยมเยียนครอบครัว",
  "villageKey": "pha-suk-village-001",
  "imageUrl": "https://example.com/visitor-photo.jpg"
}
```

**Response:**
```json
{
  "success": true,
  "message": "Sent to 2/2 residents",
  "visitorId": "visitor_1703123456789_abc123def",
  "notificationResult": {
    "success": true,
    "message": "Sent to 2/2 residents",
    "successful": 2,
    "failed": 0,
    "total": 2
  }
}
```

### 2. ส่งรายละเอียดผู้เยี่ยม
```http
POST /api/visitor-notification/send-details
Authorization: Bearer <JWT_TOKEN>
Content-Type: application/json

{
  "userId": "U1234567890abcdef",
  "visitorName": "สมชาย ใจดี",
  "visitorPhone": "081-234-5678",
  "houseNumber": "123/45",
  "residentName": "สมหญิง รักบ้าน",
  "purpose": "เยี่ยมเยียนครอบครัว",
  "villageKey": "pha-suk-village-001",
  "visitorId": "visitor_001",
  "imageUrl": "https://example.com/visitor-photo.jpg"
}
```

### 3. จัดการการตอบกลับจากผู้อยู่อาศัย
```http
POST /api/visitor-notification/handle-response
Content-Type: application/json

{
  "action": "approve",
  "visitorId": "visitor_001",
  "userId": "U1234567890abcdef",
  "reason": "ไม่พบข้อมูลผู้เยี่ยม",
  "houseNumber": "123/45",
  "villageKey": "pha-suk-village-001"
}
```

**Response:**
```json
{
  "success": true,
  "message": "Response processed successfully",
  "action": "approve",
  "visitorId": "visitor_001",
  "userId": "U1234567890abcdef",
  "confirmationMessage": "✅ คุณได้ยืนยันการเข้าให้ผู้เยี่ยมแล้ว\n\nผู้เยี่ยมสามารถเข้าบ้านได้แล้ว"
}
```

### 4. ดูผู้อยู่อาศัยในบ้าน
```http
GET /api/visitor-notification/residents/:houseNumber/:villageKey
Authorization: Bearer <JWT_TOKEN>
```

**Response:**
```json
{
  "success": true,
  "houseNumber": "123/45",
  "villageKey": "pha-suk-village-001",
  "residents": [
    {
      "residentId": "resident_001",
      "name": "สมหญิง รักบ้าน",
      "lineUserId": "U1234567890abcdef"
    },
    {
      "residentId": "resident_002",
      "name": "สมชาย รักบ้าน",
      "lineUserId": "U0987654321fedcba"
    }
  ],
  "count": 2
}
```

### 5. ทดสอบระบบ
```http
POST /api/visitor-notification/test
Authorization: Bearer <JWT_TOKEN>
Content-Type: application/json

{
  "userId": "U1234567890abcdef"
}
```

## Flex Message Templates

### 1. Visitor Notification Message
- **Header**: แจ้งเตือนผู้เยี่ยม
- **Body**: ข้อมูลผู้เยี่ยมและบ้าน
- **Footer**: ปุ่ม "ดูรายละเอียด" และ "ยืนยันการเข้า"

### 2. Visitor Details Message
- **Header**: รายละเอียดผู้เยี่ยม
- **Body**: ข้อมูลครบถ้วน + รูปภาพ
- **Footer**: ปุ่ม "ยืนยันการเข้า" และ "ปฏิเสธ"

## การใช้งาน

### 1. สำหรับรปภ.

```typescript
// ส่งแจ้งเตือนผู้เยี่ยม
const response = await fetch('/api/visitor-notification/send-to-residents', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${jwtToken}`
  },
  body: JSON.stringify({
    visitorName: 'สมชาย ใจดี',
    visitorPhone: '081-234-5678',
    houseNumber: '123/45',
    residentName: 'สมหญิง รักบ้าน',
    purpose: 'เยี่ยมเยียนครอบครัว',
    villageKey: 'pha-suk-village-001'
  })
});
```

### 2. สำหรับผู้อยู่อาศัย

เมื่อได้รับแจ้งเตือน ผู้อยู่อาศัยจะเห็น:

1. **ข้อความแจ้งเตือน** พร้อมข้อมูลผู้เยี่ยมเบื้องต้น
2. **ปุ่ม "ดูรายละเอียด"** - เปิดหน้าเว็บดูข้อมูลแบบละเอียด
3. **ปุ่ม "ยืนยันการเข้า"** - ยืนยันทันทีผ่าน LINE

เมื่อกด "ดูรายละเอียด":
1. **ข้อมูลครบถ้วน** พร้อมรูปภาพ (ถ้ามี)
2. **ปุ่ม "ยืนยันการเข้า"** - อนุญาตให้เข้าบ้าน
3. **ปุ่ม "ปฏิเสธ"** - ไม่อนุญาต พร้อมระบุเหตุผล

## การตั้งค่า

### Environment Variables
```bash
LINE_CHANNEL_ACCESS_TOKEN=your_line_channel_access_token
FRONTEND_URL=https://your-frontend-domain.com
```

### Database Schema
ระบบใช้ตาราง `house_members` เพื่อดึงข้อมูลผู้อยู่อาศัย:

```sql
-- ตัวอย่างโครงสร้างตาราง
house_members:
- house_member_id (PK)
- house_number
- village_key
- resident_id (FK)
- created_at

residents:
- resident_id (PK)
- fname
- lname
- line_user_id
- status
```

## การทดสอบ

### 1. ทดสอบผ่าน API
```bash
# ทดสอบส่งแจ้งเตือน
curl -X POST http://localhost:3001/api/visitor-notification/test \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -d '{"userId": "YOUR_LINE_USER_ID"}'
```

### 2. ทดสอบผ่าน Code
```typescript
import { visitorNotificationExamples } from './examples/visitorNotificationUsage';

// ทดสอบ workflow ครบถ้วน
await visitorNotificationExamples.completeWorkflowExample();
```

## การจัดการข้อผิดพลาด

### 1. ไม่พบผู้อยู่อาศัย
```json
{
  "success": false,
  "message": "No residents found in house"
}
```

### 2. การส่งแจ้งเตือนล้มเหลว
```json
{
  "success": false,
  "message": "Failed to send notifications",
  "error": "LINE API error"
}
```

### 3. ข้อมูลไม่ครบถ้วน
```json
{
  "success": false,
  "error": "Missing required fields: visitorName, visitorPhone, houseNumber, residentName, purpose, villageKey"
}
```

## การติดตามและ Log

ระบบจะบันทึก log สำหรับ:
- การส่งแจ้งเตือน
- การตอบกลับจากผู้อยู่อาศัย
- ข้อผิดพลาดที่เกิดขึ้น
- สถิติการใช้งาน

```typescript
// ตัวอย่าง log
console.log(`Guard ${guardId} sent visitor notification for ${visitorName} to house ${houseNumber}`);
console.log(`Resident ${userId} ${action}d visitor ${visitorId} with reason: ${reason}`);
console.log(`📊 Notification results: ${successful} successful, ${failed} failed`);
```

## การปรับแต่ง

### 1. แก้ไขข้อความ
แก้ไขใน `flexMessage.ts`:
```typescript
// เปลี่ยนข้อความใน header
text: '🔔 แจ้งเตือนผู้เยี่ยมใหม่',

// เปลี่ยนสี
color: '#1DB446',
```

### 2. เพิ่มฟิลด์ข้อมูล
เพิ่มใน `VisitorNotificationData` interface:
```typescript
interface VisitorNotificationData {
  // ... existing fields
  vehicleNumber?: string;
  expectedDuration?: string;
}
```

### 3. ปรับแต่งการตอบกลับ
แก้ไขใน `visitorNotification.ts`:
```typescript
// เปลี่ยนข้อความยืนยัน
const confirmationMessage = action === 'approve' 
  ? `✅ คุณได้ยืนยันการเข้าให้ผู้เยี่ยมแล้ว\n\nผู้เยี่ยมสามารถเข้าบ้านได้แล้ว`
  : `❌ คุณได้ปฏิเสธการเข้าให้ผู้เยี่ยมแล้ว${reason ? `\n\nเหตุผล: ${reason}` : ''}`;
```

## การรักษาความปลอดภัย

### 1. Authentication
- ใช้ JWT token สำหรับรปภ.และแอดมิน
- ตรวจสอบสิทธิ์การเข้าถึง

### 2. Rate Limiting
- จำกัดจำนวนการส่งแจ้งเตือนต่อนาที
- ป้องกันการใช้งานเกินขีดจำกัด

### 3. Data Validation
- ตรวจสอบข้อมูลที่ส่งเข้ามา
- ป้องกัน SQL injection และ XSS

## การบำรุงรักษา

### 1. การตรวจสอบสถานะ
```bash
# ตรวจสอบ LINE API
curl -H "Authorization: Bearer $LINE_CHANNEL_ACCESS_TOKEN" \
  https://api.line.me/v2/bot/info
```

### 2. การสำรองข้อมูล
- สำรองข้อมูล visitor records
- สำรองข้อมูลการตอบกลับ

### 3. การอัปเดต
- อัปเดต LINE Bot SDK
- อัปเดต Flex Message templates

## การสนับสนุน

สำหรับปัญหาหรือคำถาม:
1. ตรวจสอบ log files
2. ทดสอบด้วย test endpoint
3. ตรวจสอบ LINE Channel Access Token
4. ติดต่อทีมพัฒนา
